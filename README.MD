# CLI para tareas de IaC

## Instalación CLI
```
npm i
npm run build

# Install globally
npm i -g .
```
## Funcionalidades

El detalle del uso de cada comando está documentado en la propia herramienta
ejecutando ```aws-sso -h``` podremos ver la información
